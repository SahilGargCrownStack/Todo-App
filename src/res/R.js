import strings, { routes, stringArray } from 'res/strings';
import images from 'res/images';
import colors from 'res/colors';

export default {
    strings,
    images,
    colors,
    routes,
    stringArray
};